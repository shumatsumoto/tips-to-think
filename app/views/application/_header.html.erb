<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "Tips To Think", articles_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if @current_user %>
        <li class="nav-item">
          <%= header_link_item('記事一覧', articles_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item(@current_user.name, mypage_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item('ログアウト', logout_path, method: :delete) %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= header_link_item('新規登録', new_user_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item('ログイン', root_path) %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
