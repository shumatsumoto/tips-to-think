<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "Tips To Think", root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if @current_user %>
        <li class="nav-item">
          <%= header_link_item('Home', articles_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item('Users', users_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item('New Article', new_article_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item('Logout', logout_path, method: :delete) %>
        </li>
        <li class="nav-item">
          <%= header_link_item(@current_user.name, mypage_path) %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= header_link_item('SignUp', new_user_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item('Login', root_path) %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
